[logging]
  level = "info"
  outputPaths = ["stdout", "log"]
  errorOutputPaths = ["stderr"]
  filename = "app.log"
  encoding = "json"

[lumberjack]
  filename = "./logs/app.log"
  maxsize = 10
  maxbackups = 3
  maxage = 28
  compress = true
